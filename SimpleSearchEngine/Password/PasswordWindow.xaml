<Window x:Class="SimpleSearchEngine.Password.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Password" Height="110" Width="200"
        WindowStyle="ToolWindow"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="15"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <StackPanel Height="auto">
        <PasswordBox x:Name="pswd" KeyUp="pswd_KeyUp" BorderThickness="2" Margin="2"></PasswordBox>
        <Button Click="Button_Click" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="50" Margin="2">OK</Button>
    </StackPanel>
</Window>
